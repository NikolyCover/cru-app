import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    card: {
        backgroundColor: '#289FC3',
        borderRadius: 8,
        padding: 8
    },
    title: {
        color: '#FFF',
        fontSize: 16
    }
})